# problem2.py

def partial_print(word: str) -> None:

    sequence = range(0,len(word), 2)

    new_word = ""

    for num in sequence:
        new_word += f"^{word[num]}^"

    print(new_word)

partial_print("BooIsHappyToday!")
